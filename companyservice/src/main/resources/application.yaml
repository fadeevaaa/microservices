server:
  port: 0
spring:
  application:
    name: companyservice
  datasource:
    url: jdbc:postgresql://localhost:5432/company_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/

logging:
  level:
    root: INFO